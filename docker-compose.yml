services:
  python:
    build: .
    depends_on:
      - db
    command: python app/API_all_stats.py

  db:
    image: mysql:8.3.0
    environment:
      MYSQL_DATABASE: influencers_posts
      MYSQL_ROOT_PASSWORD: password
    ports:
      - "3306:3306"
